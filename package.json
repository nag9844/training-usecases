{
  "name": "aws-terraform-alb-ec2",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "plan": "terraform plan",
    "apply": "terraform apply",
    "destroy": "terraform destroy"
  }
}